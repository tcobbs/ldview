{
    IBClasses = (
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = LDrawModelView; LANGUAGE = ObjC; SUPERCLASS = NSOpenGLView; }, 
        {
            ACTIONS = {
                actions = id; 
                features = id; 
                open = id; 
                preferences = id; 
                reload = id; 
                saveSnapshot = id; 
                saveViewingAngle = id; 
                viewMode = id; 
                viewingAngle = id; 
            }; 
            CLASS = ModelWindow; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                actionsSegments = NSSegmentedControl; 
                featuresSegments = NSSegmentedControl; 
                modelView = LDrawModelView; 
                openButton = NSSegmentedControl; 
                prefsSegments = NSSegmentedControl; 
                progress = NSProgressIndicator; 
                progressMessage = NSTextField; 
                reloadButton = NSSegmentedControl; 
                saveAccessoryView = NSView; 
                snapshotButton = NSSegmentedControl; 
                statusBar = NSView; 
                viewModeSegments = NSSegmentedControl; 
                viewPopUp = NSPopUpButton; 
                viewingAngleSegments = NSSegmentedControl; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = NSLevelIndicator; LANGUAGE = ObjC; SUPERCLASS = NSControl; }, 
        {CLASS = NSSegmentedControl; LANGUAGE = ObjC; SUPERCLASS = NSControl; }, 
        {
            CLASS = ToolbarSegmentedControl; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSSegmentedControl; 
        }
    ); 
    IBVersion = 1; 
}