{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {ok = id; }; 
            CLASS = OpenGLDriverInfo; 
            LANGUAGE = ObjC; 
            OUTLETS = {panel = NSPanel; textField = NSTextField; textView = NSTextView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}