{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {optionChanged = id; showHideOptions = id; }; 
            CLASS = ModelTree; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                contentView = NSView; 
                drawer = NSDrawer; 
                optionsBox = NSBox; 
                optionsBoxLabel = NSTextField; 
                optionsMatrix = NSMatrix; 
                outlineView = NSOutlineView; 
                showHideOptionsButton = NSButton; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}