{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancel = id; ok = id; resetAll = id; }; 
            CLASS = Options; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                cancelButton = NSButton; 
                docView = OptionsDocView; 
                okButton = NSButton; 
                panel = NSPanel; 
                scrollView = NSScrollView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = OptionsDocView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = OptionsPanel; LANGUAGE = ObjC; SUPERCLASS = NSPanel; }
    ); 
    IBVersion = 1; 
}