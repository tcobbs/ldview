{
    IBClasses = (
        {
            ACTIONS = {
                alwaysOnTop = id; 
                boundingBox = id; 
                cancelLoad = id; 
                checkForLibraryUpdates = id; 
                customizeToolbar = id; 
                errorsAndWarnings = id; 
                examineMode = id; 
                flyThroughMode = id; 
                latLongRotation = id; 
                modelTree = id; 
                myAction = id; 
                openGLDriverInfo = id; 
                partsList = id; 
                reload = id; 
                saveSnapshot = id; 
                saveViewingAngle = id; 
                showPovCameraInfo = id; 
                showViewInfo = id; 
                takeStepFrom = id; 
                toggleFullScreen = id; 
                toggleStatusBar = id; 
                toggleToolbar = id; 
                viewingAngle = id; 
                zoomToFit = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                newWindow = id; 
                openModel = id; 
                pollingMode = id; 
                preferences = id; 
                print = id; 
                resetView = id; 
                showHelp = id; 
            }; 
            CLASS = LDViewController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                alwaysOnTopMenuItem = NSMenuItem; 
                cancelMenuItem = NSMenuItem; 
                copyMenuItem = NSMenuItem; 
                custToolbarMenuItem = NSMenuItem; 
                examineMenuItem = NSMenuItem; 
                fileMenu = NSMenu; 
                flyThroughMenuItem = NSMenuItem; 
                goMenu = NSMenu; 
                latLongRotationMenuItem = NSMenuItem; 
                modelTreeMenuItem = NSMenuItem; 
                pollingAutoLaterMenuItem = NSMenuItem; 
                pollingAutoNowMenuItem = NSMenuItem; 
                pollingDisabledMenuItem = NSMenuItem; 
                pollingPromptMenuItem = NSMenuItem; 
                prefsMenuItem = NSMenuItem; 
                statusBarMenuItem = NSMenuItem; 
                toolbarMenuItem = NSMenuItem; 
                viewingAngleMenu = NSMenu; 
            }; 
            SUPERCLASS = NSResponder; 
        }, 
        {CLASS = LDrawModelView; LANGUAGE = ObjC; SUPERCLASS = NSOpenGLView; }
    ); 
    IBVersion = 1; 
}